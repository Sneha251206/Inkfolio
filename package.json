{
  "name": "inkfolio-root",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd Inkfolio/frontend && npm install && npm run build"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
